apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ethBalanceProxy.fullname" . }}-test-connection"
  labels:
    {{- include "ethBalanceProxy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ethBalanceProxy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
